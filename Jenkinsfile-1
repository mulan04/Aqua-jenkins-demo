
pipeline {
    agent {
        node {
            label 'docker-agent'
        }
    }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}
